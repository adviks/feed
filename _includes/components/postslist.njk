<section>
    {% for post in postslist | reverse %}
        <div {% if post.url == url %} data-current="current item"{% endif %} class="post_list">
            <a class="post" href="{{ post.url | url }}">
                <div class="text_content">
                    <h2>{{ post.data.title }}</h2>
                    <p style="margin-top:0">
                            {{ post.templateContent | striptags | truncateWords(25) }}
                    </p>
                </div>
                <div class="thumbnail">
                    <img class="post_thumbnail" src="{{ post.data.thumbnail | default('https://placehold.co/600x400') }}" alt="{{ post.data.title }}">
                </div>
            </a>
        </div>
    {% endfor %}
</section>





<style>

.post {
    display: flex;
    justify-content: space-between;  
    align-items: center;
    text-decoration: none; 
}
.post_list{
margin-bottom:10px;
}

.post_thumbnail {
    width: 120px;
  height: 67px; 
    object-fit: cover;
    border-radius: 5px;
}

.text_content {
    flex: 1;
    margin-right: 10px; 
    color: var(--text-color);
}

.text_content h2 {
    font-size: 21px;
    margin-bottom: 5px;
    border-bottom: 1px solid var(--link-color);
    display: inline-block;
    font-family: "Instrument Serif";
    font-weight: normal;
    color: var(--text-color);
}

.text_content p {
    font-size: 14px;
}
</style>
